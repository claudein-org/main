
interface Props {
    likes?: number
    comments?: number
    impressions?: number
    children?: React.ReactNode
}
export default function LinkedInPost({ children }: Props) {
    // TODO: create a component that mock a Linkedin post use /gilad.jpeg as the avatar and use the children as the post content. Add likes, comments, and impressions to the bottom of the post.
    return null
}