Introduction:
  - This is a web app that has an API to publish posts to linkedin.
  - A new user login to the app using their google account.
  - After a successful login, the user can connect their LinkedIn account.
  - The service has a web app and a local cli client:
      - All the data is managed by the client and the web app serves as a UI and as a proxy to the LinkedIn API.
      - the cli and web communicate via websockets.
      - client manage the data in a claudein.yml file.
      - the web only saves post_id, post_url (after the post is published) and post_date (when it was published).
      - client can query the web app for all the history of published posts.
      - client can send to the UI new posts, the UI then shows a preview of the post and lets the user edit, delete or publish it (if the user edit/delete the post the cli updates the local file).
      - local file can be edited by user and external tools (e.g. claud code) directly, the cli will watch for changes and update the web app accordingly.

TODO:
  - providers:
  - common:
  - cli:
      - file system structure:
          - refactor the structure of the local files the cli expected:
          - claudein:
              - claudein.yml (all the information about the brand excluding media and .md files)
              - media:
                  - file.jpg
                  - file.mp4
                  - ...
              - articles:
                  - file.md
                  - ...

          - if the file structure does not exists, create it and create populate it with the default claudein.yml file and example article.
          - update the SKILL.md file to reflect the new file structure.
  - web:
  - video:

Done:
  - Articles:
      - I updated the schema to support articles, you need to update the reset of the code (both cli and web) to support this:
          - cli:
              - watch referenced .md files for changes
              - load the content of the .md file and send it to the web app
              - add the content of the .md to hash parts.
          - web:
              - add another section for articles in the sidebar
              - add a (use react-markdown) preview responsive grid for articles (2 on desktop, 1 on mobile)

      - When done write a short article about claudein and add it to the .yml file.
  - Add a post to brand.yml: "ClaudeIn turns your Claude Code into a social media manager."
  - YouTube - let users add multiple channels, then use their channel profile picture on the post button so they know which channel they are posting to.
