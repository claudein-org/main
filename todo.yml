Introduction:
  - This is a web app that has an API to publish posts to linkedin.
  - A new user login to the app using their google account.
  - After a successful login, the user can connect their LinkedIn account.
  - After a successful connection, the user can download a JSON file with their API key which they can use from a command line tool to interact with the web app.

TODO:
Done:
  - Use the logo.svg in the home page and also use it in the favicon.
  - Implement the google login flow, after a successful login add the user to the database and set a signed cookie with the user id.
  - replace the design of the app use a clean, claude.ai like design and combine the color scheme from claude.ai and LinkedIn.com
  - I added an api_key column to the user table and removed the api table. Please fix the project accordantly. When user logged in for the first time generate a random  api key for them.
    Currently the project is broken, fix all errors as well.
    After this task the web app should -
    - 1. Allow users to login and go to their dashboard.
    - 2. Allow users to connect their linkedin account.
    - 3. Allow users to download a JSON file with their API key.
