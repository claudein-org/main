Linkedin:
  # - https://forms.office.com/Pages/ResponsePage.aspx?id=v4j5cvGGr0GRqy180BHbR7Dqh0PdJfxGkDEwKUJVJ9xUOENGMFYwTU01WDZPUjVKQTlQWFZYWkk1ViQlQCN0PWcu

TODO:
  - providers:
  - common:
  - web:
      - analytics:
  - video:

Done:
  - web: Analytics bar chart improvements — x-axis date labels (every 7 days), SVG-native hover tooltip showing date + per-provider counts, dimming of non-hovered bars.
  - web: Analytics top posts — replaced leaderboard with a proper table (rank, provider, link, date, impressions, engagement). Added `impressions` + `post_date` to `TopPost` and query.
  - web: Read posts.md + analytics.md and generate an operative plan for the analytics dashboard (provider-API-pulled metrics, new DB schema, cron sync, phased rollout). Full report written to `plan.v1.md`.
  - web: Research how to track published posts and propose a better DB schema (provider post ids + multi-account + optional sync of all posts). Report written to `posts.md`.
  - web: remember my last tab (from the side bar) in a cookie then load it on page load.
  - web: Add analytics tab to the sidebar. The analytics view is empty for now, add a placeholder for it.
